# we want to code taxi algo here

# RESHAPE

# PREPARE data to do and process for establishing the analytic data

# establish ANALYTIC DATA

# calculate the indicator stats



reshape_txxt <- function(apdf){
  # intentionally left blank
}

prepare_txxt <- function(apdf, reft, phase){
  # append apdf with provided reference times per stand-runway grouping

  # calculate the additional time per flight, phase,
}

establish_analytic_txxt <- function(apdf, ...){
  # per PBWG convention, the analytic data
  # aggregates on a daily basis, per phase, sum(TXOT or TXIT), and sum(REFT)
}

determine_txxt_stats <- function(...){
  # based on analytic data generate respective stats
}
